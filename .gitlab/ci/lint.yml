
fmt:
  extends: .rust-base
  stage: pre-check
  script:
    - rustup component add rustfmt
    - cargo fmt --check

clippy:
  extends: .rust-base
  stage: verify
  needs:
    - job: build
      artifacts: true
  script:
    - rustup component add clippy
    - cargo clippy -- -D warnings
