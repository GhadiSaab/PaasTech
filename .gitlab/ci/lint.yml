
fmt:
  extends: .rust-base
  stage: pre-check
  script:
    - rustup component add rustfmt
    - cargo fmt --check

clippy:
  extends:
    - .rust-base
    - .verify-cache
  stage: verify
  script:
    - rustup component add clippy
    - cargo clippy -- -D warnings
